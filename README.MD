# SETUP
### Requirements
- python3 -m venv .venv
- source .venv/bin/activate
- pip3 install -r requirements.txt

### .env SETUP
- Take .env_example as a template:
    - Set DB URL 
    - Set SECRET KEY

### RUN main.py